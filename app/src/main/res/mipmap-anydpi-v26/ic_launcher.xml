<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@drawable/ic_launcher_background" />
    <foreground android:drawable="@drawable/ic_launcher_foreground_inset" />
    <!-- Android 13+ themed (monochrome) icon: the system tints the art's silhouette to match the
         user's wallpaper/theme. Reuses the same inset art so it stays centered. -->
    <monochrome android:drawable="@drawable/ic_launcher_foreground_inset" />
</adaptive-icon>
